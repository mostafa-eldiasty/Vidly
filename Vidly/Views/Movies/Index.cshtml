@model IEnumerable<Vidly.Models.Movie>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Movies</h2>

<table class="table">
    <tr>
        <th>Movie</th>
        <th>Genre</th>
    </tr>

@foreach (var movie in Model) {
<tr>
    <td><a href="/Movies/Details/@movie.Id">@Html.DisplayFor(modelmovie => movie.Name)</a></td>
    <td>@Html.DisplayFor(modelItem => movie.Genre.Name)</td>
</tr>
}

</table>
